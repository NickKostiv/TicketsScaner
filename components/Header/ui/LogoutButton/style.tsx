import { Colors } from "@/constants/Colors";
import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
  logoutButton: { fontSize: 16, fontWeight: "bold", paddingVertical: 10, paddingHorizontal: 20, color: Colors.light.white, backgroundColor: Colors.light.primary, borderRadius: 100 },
});